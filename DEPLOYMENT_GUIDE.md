# 🚀 คู่มือการ Deploy และเชื่อมต่อ Google Sheets กับหน้าเว็บ

## 📋 ขั้นตอนที่ 1: เตรียม Google Sheets

### 1.1 สร้าง Google Spreadsheet ใหม่
1. เปิด [Google Sheets](https://sheets.google.com)
2. สร้าง Spreadsheet ใหม่ (ตั้งชื่อว่า "Oxford 3000 Flashcards")
3. Copy **Spreadsheet ID** จาก URL:
   ```
   https://docs.google.com/spreadsheets/d/[SPREADSHEET_ID]/edit
   ```

### 1.2 สร้าง 3 Sheets

#### Sheet 1: `words`
```
| id | word | Parts_of_Speech | Level | translation |
|----|------|-----------------|-------|-------------|
```
- **id**: ปล่อยว่างได้ (จะ gen อัตโนมัติ)
- **word**: ใส่คำภาษาอังกฤษ เช่น "abandon v. B2"
- **Parts_of_Speech**: ปล่อยว่างได้ (จะแยกจาก word)
- **Level**: ปล่อยว่างได้ (จะแยกจาก word)
- **translation**: ปล่อยว่างได้ (จะแปลอัตโนมัติ)

**ตัวอย่างข้อมูล:**
```
word
abandon v. B2
ability n. A2
abroad adv. A2
absolute adj. B2
```

#### Sheet 2: `user_state`
```
| user_id | word_id | learned | hidden_forever | repetitions | interval | ef | next_due | updated_at |
|---------|---------|---------|----------------|-------------|----------|----|---------|-----------| 
```
ปล่อยว่างไว้ (ระบบจะเติมเอง)

#### Sheet 3: `users`
```
| user_id | username | password |
|---------|----------|----------|
```

**ตัวอย่างข้อมูล:**
```
user_id        username    password
user-0001      john        pass1234
user-0002      mary        mypass99
```

---

## ⚙️ ขั้นตอนที่ 2: Deploy Google Apps Script

### 2.1 เปิด Apps Script Editor
1. ใน Google Sheets ไปที่ **Extensions → Apps Script**
2. ลบโค้ดเดิมทิ้ง
3. Copy โค้ดทั้งหมดจาก `Code.gs` มาวาง

### 2.2 แก้ไข CONFIG
```javascript
const CONFIG = {
  SPREADSHEET_ID: '1iiaWg7i53ryMLbUk3fZTGyTL-SiBnM1vtis6eA-CCUs', // ⬅️ ใส่ ID จากขั้นตอนที่ 1.1
  SHEET_WORDS: 'words',
  SHEET_USER_STATE: 'user_state',
  SHEET_USERS: 'users',
  CORS_ORIGIN: 'https://YOUR_USERNAME.github.io', // ⬅️ จะแก้ทีหลัง (ขั้นตอนที่ 4.3)
  API_KEY: 'my-super-secret-key-12345' // ⬅️ สร้าง random string ยาวๆ
};
```

### 2.3 Deploy as Web App
1. คลิก **Deploy → New deployment**
2. เลือก type: **Web app**
3. ตั้งค่า:
   - **Description**: Oxford 3000 Flashcards API
   - **Execute as**: **Me** (ตัวเอง)
   - **Who has access**: **Anyone** (ทุกคน)
4. คลิก **Deploy**
5. **Authorize** (อนุญาตให้เข้าถึง Sheets)
6. Copy **Deployment ID** (ลิงก์จะเป็น: `https://script.google.com/macros/s/[DEPLOYMENT_ID]/exec`)

**ตัวอย่าง Deployment URL:**
```
https://script.google.com/macros/s/AKfycbxNwerSDLj8cFX6HIIcnFYYvhPyohFL5eUnMoZ4jXvEIP1bF-ByZJw9IJT2pWbVh5HctQ/exec
```

---

## 💻 ขั้นตอนที่ 3: แก้ไข Frontend

### 3.1 แก้ไข `app.js`
เปิดไฟล์ `app.js` แล้วแก้ CONFIG:

```javascript
const CONFIG = {
    API_URL: 'https://script.google.com/macros/s/AKfycbxNwerSDLj8cFX6HIIcnFYYvhPyohFL5eUnMoZ4jXvEIP1bF-ByZJw9IJT2pWbVh5HctQ/exec', // ⬅️ ใส่ Deployment URL จากขั้นตอนที่ 2.3
    API_KEY: 'my-super-secret-key-12345', // ⬅️ ต้องตรงกับใน Code.gs
    USER_ID_KEY: 'flash_user_id',
    AUTH_KEY: 'flash_auth_data'
};
```

---

## 🌐 ขั้นตอนที่ 4: Deploy บน GitHub Pages

### 4.1 สร้าง GitHub Repository
1. ไปที่ [GitHub](https://github.com)
2. คลิก **New repository**
3. ตั้งชื่อ: `oxford-3000-flashcards`
4. เลือก **Public**
5. คลิก **Create repository**

### 4.2 Upload ไฟล์
Upload 3 ไฟล์หลัก:
- `index.html`
- `app.js` (ที่แก้แล้วในขั้นตอนที่ 3)
- `styles.css`

### 4.3 เปิด GitHub Pages
1. ไปที่ **Settings → Pages**
2. เลือก Source: **Deploy from a branch**
3. เลือก Branch: **main** (หรือ **master**)
4. คลิก **Save**
5. รอ 1-2 นาที จะได้ URL:
   ```
   https://YOUR_USERNAME.github.io/oxford-3000-flashcards/
   ```

### 4.4 อัพเดท CORS_ORIGIN
1. กลับไปที่ **Google Apps Script**
2. แก้ `CORS_ORIGIN` ใน CONFIG:
   ```javascript
   CORS_ORIGIN: 'https://YOUR_USERNAME.github.io'
   ```
3. **Deploy → New deployment** อีกครั้ง (หรือ **Manage deployments → Edit → Version: New version**)

---

## ✅ ขั้นตอนที่ 5: ทดสอบระบบ

### 5.1 ทดสอบ Login
1. เปิดหน้าเว็บที่ได้จาก GitHub Pages
2. ใส่ username/password จาก sheet `users`
3. ถ้าเข้าได้ = เชื่อมต่อสำเร็จ! 🎉

### 5.2 ทดสอบแสดงคำศัพท์
1. คลิกปุ่ม **🚀 เริ่มต้น**
2. ควรแสดงคำศัพท์จาก sheet `words`
3. ทดสอบปุ่ม:
   - **💬** แสดง/ซ่อนคำแปล
   - **✅ จำได้แล้ว** → ไม่แสดงคำนี้อีก
   - **🙈 ซ่อนถาวร** → ซ่อนคำ
   - **➡️ ถัดไป** → ไปคำต่อไป

### 5.3 ทดสอบ Auto-Generate
1. เพิ่มคำใหม่ใน sheet `words` (ใส่แค่คอลัมน์ word):
   ```
   word
   accept v. A2
   ```
2. รีเฟรชหน้าเว็บ
3. ตรวจสอบใน sheet `words` จะเห็น:
   - **id** ถูกสร้างอัตโนมัติ
   - **Parts_of_Speech** = "v."
   - **Level** = "A2"
   - **translation** = "ยอมรับ" (แปลอัตโนมัติ)

---

## 🔧 Troubleshooting

### ❌ ปัญหา: Login ไม่ได้
**แก้ไข:**
- ตรวจสอบ sheet `users` มีข้อมูล username/password
- ตรวจสอบ `API_URL` ใน `app.js` ถูกต้อง
- เปิด Console (F12) ดู error

### ❌ ปัญหา: ไม่แสดงคำศัพท์
**แก้ไข:**
- ตรวจสอบ sheet `words` มีข้อมูล
- ตรวจสอบชื่อ sheet เป็น `words` (ตัวพิมพ์เล็ก)
- ตรวจสอบ `SPREADSHEET_ID` ใน `Code.gs` ถูกต้อง

### ❌ ปัญหา: CORS Error
**แก้ไข:**
- ตรวจสอบ `CORS_ORIGIN` ใน `Code.gs` ตรงกับ GitHub Pages URL
- ต้อง deploy ใหม่หลังแก้ `CORS_ORIGIN`

### ❌ ปัญหา: API_KEY Invalid
**แก้ไข:**
- ตรวจสอบ `API_KEY` ใน `Code.gs` และ `app.js` ตรงกัน
- ใช้ตัวอักษร, ตัวเลข, `-` เท่านั้น (ไม่ใช้อักขระพิเศษ)

---

## 📊 สรุปไฟล์และการตั้งค่า

| ไฟล์/ที่ตั้ง | ต้องแก้ไข | รายละเอียด |
|-------------|----------|-----------|
| Google Sheets | ✅ | สร้าง 3 sheets: words, user_state, users |
| Code.gs | ✅ | SPREADSHEET_ID, API_KEY, CORS_ORIGIN |
| app.js | ✅ | API_URL, API_KEY |
| index.html | ❌ | ไม่ต้องแก้ |
| styles.css | ❌ | ไม่ต้องแก้ |

---

## 🎯 การอัพเดทข้อมูล

### เพิ่มคำศัพท์ใหม่:
1. เปิด Google Sheets
2. ใน sheet `words` เพิ่มแถวใหม่
3. ใส่เฉพาะคอลัมน์ `word` เช่น "achieve v. A2"
4. ระบบจะ generate ID, POS, Level, translation อัตโนมัติ

### เพิ่ม User ใหม่:
1. เปิด sheet `users`
2. เพิ่มแถวใหม่:
   ```
   user_id        username    password
   user-0003      alice       alice123
   ```
3. User สามารถ login ได้ทันที

### แก้ไขคำแปล:
1. เปิด sheet `words`
2. แก้ไขคอลัมน์ `translation` ได้เลย
3. รีเฟรชหน้าเว็บจะเห็นการเปลี่ยนแปลง

---

## 🔐 Security Best Practices

1. **API_KEY**: ใช้ random string ยาว 20+ ตัวอักษร
2. **Password**: แนะนำให้ผู้ใช้เปลี่ยน password หลัง login ครั้งแรก
3. **Sheets Permissions**: ตั้งค่าให้เฉพาะคนที่จำเป็นเข้าถึงได้
4. **Deploy Settings**: ตรวจสอบ "Execute as: Me" เพื่อความปลอดภัย

---

## 📝 Checklist

- [ ] สร้าง Google Sheets พร้อม 3 sheets
- [ ] ใส่ข้อมูล users และ words
- [ ] Copy SPREADSHEET_ID
- [ ] Deploy Apps Script และได้ Deployment URL
- [ ] แก้ CONFIG ใน Code.gs (SPREADSHEET_ID, API_KEY)
- [ ] แก้ CONFIG ใน app.js (API_URL, API_KEY)
- [ ] Upload ไฟล์ไป GitHub
- [ ] เปิด GitHub Pages
- [ ] แก้ CORS_ORIGIN ใน Code.gs
- [ ] Deploy Apps Script ใหม่
- [ ] ทดสอบ login และแสดงคำศัพท์

---

✨ **เสร็จสิ้น! ระบบพร้อมใช้งาน**
